name: format-lint-unittest

on: 
  - push
  - pull_request

jobs:
  format-lint-unittest:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2.3.4
    - uses: conda-incubator/setup-miniconda@v2
      with:
        activate-environment: your_project_name
        environment-file: environment.yml
        python-version: 3.6.12  # Change python version if necessary
        auto-activate-base: false
    - shell: bash -l {0}
      run: |
        conda info
        conda list
        # - name: Format with black, isort
        #   shell: bash -l {0}
        #   run: make format
        # - name: Lint with pylint, mypy, flake8
        #   shell: bash -l {0}
        #   run: make lint
        # - name: Test with pytest
        #   shell: bash -l {0}
        #   run: make test
